-- 0143: applied via SQL editor. stories audio columns + filter_id, story_sounds
-- table + RLS, increment_sound_use, get_user_stories recreated with audio and
-- filter columns, story-media bucket audio mime types.
select 1;